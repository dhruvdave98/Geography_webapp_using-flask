# Geography_webapp_using-flask
Demonstrates the application, which loads any csv file and this file should contain address column in it, according to that,
program will generate new csv file with latitude and longitude of that address
This web app has been developed using flask.
